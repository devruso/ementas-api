Place SIGAA bootstrap snapshot files here for offline production imports.

Recommended default path:
- `bootstrap-data/sigaa-bootstrap.snapshot.json`

Generate locally with:
- `npm run sigaa:bootstrap-snapshot -- --graduacaoSourceIds=1118,1935,1934 --mestradoSourceIds=1820 --output=bootstrap-data/sigaa-bootstrap.snapshot.json`
